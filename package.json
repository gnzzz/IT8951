{
  "name": "it8951",
  "version": "0.0.1",
  "description": "Raspberry Pi nodejs module for e-papers controlled by IT8951",
  "main": "dist/it8951.js",
  "types": "dist/it8951.d.ts",
  "scripts": {
    "prebuild": "node-gyp rebuild",
    "build": "tsc",
    "test": "node --napi-modules index.js",
    "doc": "npx typedoc --plugin typedoc-plugin-markdown --excludePrivate --excludeNotExported",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "author": "K. Johan Paulsson",
  "license": "GPL-3.0-only",
  "devDependencies": {
    "@types/node": "^14.0.23",
    "node-addon-api": "^3.0.1",
    "node-gyp": "^7.0.0",
    "typedoc": "^0.18.0",
    "typedoc-plugin-markdown": "^2.4.2",
    "typescript": "^3.9.7",
    "gts": "^2.0.2"
  },
  "engines": {
    "node": ">=8.9.0"
  },
  "gypfile": true,
  "dependencies": {}
}
